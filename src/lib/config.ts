// Shared with the Node monitor server — the real values live in src/data/config.mjs.
export {
  COUNTRIES,
  COUNTRY_BY_CODE,
  FX_RATES,
  OFFER_CURRENCIES,
  TRACKED_SITES,
  importRateFor,
  CURRENCY_SYMBOL,
  formatMoney,
} from '../data/config.mjs';
